package com.kruzvinicius.limsbackend.model.enums;

/** Type of maintenance performed on equipment. */
public enum MaintenanceType {
    /** Scheduled routine maintenance to prevent failures. */
    PREVENTIVE,
    /** Unscheduled maintenance to fix an existing failure. */
    CORRECTIVE
}
