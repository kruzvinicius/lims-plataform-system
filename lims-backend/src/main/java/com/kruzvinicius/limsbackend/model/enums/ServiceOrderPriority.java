package com.kruzvinicius.limsbackend.model.enums;

/** Priority level for a service order, used for SLA and queue ordering. */
public enum ServiceOrderPriority {
    LOW,
    NORMAL,
    HIGH,
    URGENT
}
